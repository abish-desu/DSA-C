Algorithms
check if the stack is full
if the stack is full , produces an error and exit
if the stack is not full, increment top to point next empty space
adds data elements to the stack location, where top is pointing
returns success
